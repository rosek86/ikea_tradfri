{
  "name": "ikea_tradfri",
  "version": "1.0.0",
  "description": "IKEA Tradfri tools",
  "main": "firmware-check.js",
  "scripts": {
    "start": "env $(cat .env | xargs) node firmware-check.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@slack/client": "^4.3.1"
  }
}
